import { createTheme } from '@mui/material';

export const catClassTheme = createTheme({
  palette: {
    primary: {
      main: '#262254'
    },
    secondary: {
      main: '#543884'
    },
    error: {
      main: '#ff1744'
    }
  }
});